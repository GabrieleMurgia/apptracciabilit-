_schema-version: "3.2"
ID: apptracciabilitaapptracciabilita
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: apptracciabilitaapptracciabilita-destination-content
  type: com.sap.application.content
  requires:
  - name: apptracciabilitaapptracciabilita-destination-service
    parameters:
      content-target: true
  - name: apptracciabilitaapptracciabilita-repo-host
    parameters:
      service-key:
        name: apptracciabilitaapptracciabilita-repo-host-key
  - name: apptracciabilitaapptracciabilita-uaa
    parameters:
      service-key:
        name: apptracciabilitaapptracciabilita-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: apptracciabilitaapptracciabilita_html_repo_host
          ServiceInstanceName: apptracciabilitaapptracciabilita-html5-service
          ServiceKeyName: apptracciabilitaapptracciabilita-repo-host-key
          sap.cloud.service: apptracciabilitaapptracciabilita
        - Authentication: OAuth2UserTokenExchange
          Name: apptracciabilitaapptracciabilita_uaa
          ServiceInstanceName: apptracciabilitaapptracciabilita-xsuaa-service
          ServiceKeyName: apptracciabilitaapptracciabilita-uaa-key
          sap.cloud.service: apptracciabilitaapptracciabilita
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: apptracciabilitaapptracciabilita-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: apptracciabilitaapptracciabilita-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - apptracciabilitaapptracciabilita.zip
      name: apptracciabilitaapptracciabilita
      target-path: resources/
- name: apptracciabilitaapptracciabilita
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: apptracciabilitaapptracciabilita-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: apptracciabilitaapptracciabilita-destination-service
    service-plan: lite
- name: apptracciabilitaapptracciabilita-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: apptracciabilitaapptracciabilita-xsuaa-service
    service-plan: application
- name: apptracciabilitaapptracciabilita-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: apptracciabilitaapptracciabilita-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
